<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sao Chép Ảnh Cục Bộ - Tiện Ích</title>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Nunito:wght@400;600;700;800&display=swap" rel="stylesheet">
    
    <style>
        :root {
            --primary-green: #147a30; 
            --dark-green: #0e5a22; 
            --bg-color: #1a1a1a;
            --text-color: #f0f0f0;
            --accent-color: #51ff7d;
        }

        body { 
            margin: 0; padding: 0; 
            font-family: 'Nunito', sans-serif; 
            background-color: var(--bg-color); 
            /* Nền chấm bi đồng bộ */
            background-image: radial-gradient(#333 15%, transparent 16%), radial-gradient(#333 15%, transparent 16%); 
            background-size: 60px 60px; 
            background-position: 0 0, 30px 30px; 
            color: #333; 
            min-height: 100vh; 
            display: flex; 
            flex-direction: column; 
            padding-top: 80px; 
        }

        /* ⭐ HEADER CHUẨN BOOTSTRAP ⭐ */
        .navbar {
            background: var(--primary-green);
            box-shadow: 0 4px 20px rgba(0,0,0,0.3);
            backdrop-filter: blur(10px);
        }
        
        .navbar-brand {
            font-weight: 800;
            font-size: 1.5rem;
            color: white !important;
            text-transform: uppercase;
            letter-spacing: 1px;
        }

        .nav-link {
            color: rgba(255,255,255,0.9) !important;
            font-weight: 600;
            margin: 0 5px;
            transition: all 0.3s;
            border-radius: 8px;
        }

        .nav-link:hover, .nav-link.active {
            background-color: rgba(255,255,255,0.2);
            color: #fff !important;
            transform: translateY(-2px);
        }
        
        .navbar-toggler {
            border: none;
            color: white;
            padding: 0.5rem;
        }
        .navbar-toggler:focus {
            box-shadow: none;
            outline: 2px solid var(--accent-color);
        }

        /* MAIN CONTAINER */
        .main-container { 
            flex: 1; 
            display: flex; 
            justify-content: center; 
            align-items: flex-start; /* Để card không bị kéo dãn */
            padding: 30px 20px; 
        }
        
        .copy-card { 
            background: rgba(255, 255, 255, 0.98); 
            width: 100%; 
            max-width: 700px; 
            border-radius: 20px; 
            padding: 40px; 
            box-shadow: 0 20px 50px rgba(0,0,0,0.5); 
            color: #333; 
        }

        h2 { 
            text-align: center; 
            margin-top: 0; 
            margin-bottom: 30px; 
            font-weight: 800; 
            color: var(--primary-green);
        }

        /* DIR SELECTION */
        .dir-selection { 
            display: grid; 
            grid-template-columns: 1fr 1fr; 
            gap: 20px; 
            margin-bottom: 30px; 
        }

        .dir-item { 
            text-align: center; 
            background: #f8f9fa;
            padding: 15px;
            border-radius: 12px;
            border: 2px dashed #ccc;
            transition: all 0.3s;
        }
        
        .dir-item:hover {
            border-color: var(--primary-green);
            background: #fff;
            box-shadow: 0 5px 15px rgba(0,0,0,0.1);
        }

        .dir-item button { 
            background: linear-gradient(135deg, #147a30, #0e5a22); 
            color: white; 
            border: none; 
            padding: 10px 20px; 
            border-radius: 50px; 
            cursor: pointer; 
            font-weight: 700; 
            font-family: 'Nunito', sans-serif;
            transition: 0.3s; 
            box-shadow: 0 4px 10px rgba(20, 122, 48, 0.3);
            margin-bottom: 10px;
        }
        
        .dir-item button:hover { 
            transform: translateY(-2px);
            box-shadow: 0 6px 15px rgba(20, 122, 48, 0.4);
        }
        
        .dir-display { 
            font-size: 14px; 
            min-height: 20px; 
            word-break: break-all;
            font-weight: 600;
        }

        /* FORM ELEMENTS */
        label { 
            display: block; 
            margin-bottom: 10px; 
            font-weight: 700; 
            color: #444; 
            font-size: 16px; 
        }

        textarea { 
            width: 100%; 
            padding: 15px; 
            border: 2px solid #e0e0e0; 
            border-radius: 12px; 
            box-sizing: border-box; 
            margin-bottom: 25px; 
            resize: vertical;
            font-family: 'Nunito', sans-serif; /* Hoặc monospace nếu muốn */
            font-size: 1rem;
            transition: border-color 0.3s;
            min-height: 120px;
        }
        textarea:focus { border-color: var(--primary-green); outline: none; }
        
        /* COPY BUTTON */
        #copyButton { 
            background: linear-gradient(135deg, #007bff, #0056b3); 
            color: white; 
            border: none; 
            padding: 15px 30px; 
            font-size: 18px; 
            border-radius: 12px; 
            cursor: pointer; 
            width: 100%; 
            font-weight: 800; 
            transition: all 0.3s; 
            box-shadow: 0 10px 20px rgba(0, 123, 255, 0.3);
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 10px;
        }
        
        #copyButton:hover:not(:disabled) { 
            transform: translateY(-2px);
            box-shadow: 0 15px 30px rgba(0, 123, 255, 0.4);
            filter: brightness(1.1);
        }

        #copyButton:disabled { 
            background: #ccc; 
            cursor: not-allowed; 
            box-shadow: none;
            transform: none;
        }

        /* STATUS & WARNING */
        #statusDisplay { 
            margin-top: 25px; 
            padding: 15px; 
            border-radius: 10px; 
            text-align: center; 
            font-weight: 700; 
            background: #f8f9fa;
            border: 1px solid #e9ecef;
            min-height: 50px;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .warning-box { 
            background: #fff3cd; 
            color: #856404; 
            padding: 15px; 
            border-radius: 10px; 
            font-size: 14px; 
            text-align: center; 
            margin-top: 25px; 
            border: 1px solid #ffeeba; 
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 10px;
        }

        /* Mobile Adjust */
        @media (max-width: 768px) {
            .copy-card { padding: 30px 20px; }
            .dir-selection { grid-template-columns: 1fr; } /* Chuyển thành 1 cột */
            h2 { font-size: 1.5rem; }
            
             .user-info-mobile {
                border-top: 1px solid rgba(255,255,255,0.1);
                margin-top: 15px;
                padding-top: 15px;
                justify-content: flex-end;
            }
        }
    </style>
</head>
<body>

    <nav class="navbar navbar-expand-lg fixed-top navbar-dark">
        <div class="container-fluid px-4">
            <a class="navbar-brand" href="#"><i class="fas fa-copy me-2"></i>Copy Ảnh</a>
            
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarContent" aria-controls="navbarContent" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>

            <div class="collapse navbar-collapse" id="navbarContent">
                <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                    <li class="nav-item"><a href="admin.html" class="nav-link"><i class="fas fa-plus-circle"></i> Tạo Album</a></li>
                    <li class="nav-item"><a href="copy_local.html" class="nav-link active"><i class="fas fa-copy"></i> Sao Chép Ảnh</a></li>
                    <li class="nav-item"><a href="donate.html" class="nav-link"><i class="fas fa-hand-holding-usd"></i> Donate</a></li>
                    <li class="nav-item"><a href="lienhe.html" class="nav-link"><i class="fas fa-envelope"></i> Liên Hệ</a></li>
                    <li class="nav-item"><a href="managa.html" class="nav-link"><i class="fas fa-folder-open"></i> Quản Lý</a></li>
                </ul>
                
                 <div id="authControl" style="display: none;" class="d-flex align-items-center gap-3 user-info-mobile">
                    <span id="userEmailDisplay" class="text-white fw-bold px-3 py-2 rounded-pill bg-white bg-opacity-10 border border-white border-opacity-25">
                        <i class="fas fa-user-circle me-2"></i>Admin
                    </span>
                    <a href="javascript:void(0);" onclick="logoutAdmin()" class="btn btn-danger fw-bold d-flex align-items-center gap-2 rounded-pill px-3">
                        <i class="fas fa-sign-out-alt"></i> Đăng Xuất
                    </a>
                </div>
            </div>
        </div>
    </nav>

    <div class="main-container">
        <div class="copy-card">
            <h2><i class="fas fa-exchange-alt me-2"></i>Sao Chép Ảnh Nội Bộ</h2>

            <div class="dir-selection">
                <div class="dir-item">
                    <button onclick="selectDirectory('source')"><i class="fas fa-folder-open me-2"></i>Chọn Thư mục Nguồn</button>
                    <div id="sourceDirDisplay" class="dir-display text-danger mt-2"><i class="fas fa-times-circle me-1"></i>Chưa chọn nguồn</div>
                </div>
                
                <div class="dir-item">
                    <button onclick="selectDirectory('destination')"><i class="fas fa-folder-plus me-2"></i>Chọn Thư mục Đích</button>
                    <div id="destDirDisplay" class="dir-display text-success mt-2"><i class="fas fa-times-circle me-1"></i>Chưa chọn đích</div>
                </div>
            </div>

            <label for="filenamesInput"><i class="fas fa-list me-2 text-primary"></i>Danh sách tên file (cách nhau dấu phẩy)</label>
            <textarea id="filenamesInput" rows="5" placeholder="Ví dụ: IMG_001.JPG, DSC_0522.NEF, IMG_9999.PNG..."></textarea>

            <button id="copyButton" onclick="startCopyProcess()">
                <i class="fas fa-clone"></i> Thực Hiện Sao Chép
            </button>
            
            <div id="statusDisplay"><i class="fas fa-info-circle me-2 text-muted"></i>Sẵn sàng. Vui lòng chọn thư mục và nhập tên file.</div>
            
            <div class="warning-box">
                <div>
                    <i class="fas fa-exclamation-triangle fa-2x me-2 text-warning"></i>
                </div>
                <div>
                    <strong>Lưu ý quan trọng:</strong> Chức năng này sử dụng công nghệ mới, chỉ hoạt động ổn định trên trình duyệt <strong>Google Chrome</strong> hoặc <strong>Microsoft Edge</strong> trên máy tính.
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        // *************************************************************
        // LOGIC AUTH & NAVIGATION (GIỐNG CÁC TRANG KHÁC)
        // *************************************************************
        const adminEmail = localStorage.getItem('admin_email');
        const adminName = localStorage.getItem('admin_name') || 'Admin';
        const isLoggedIn = localStorage.getItem('admin_is_logged_in') === 'true';

        function updateHeaderUI() {
            const authControl = document.getElementById('authControl');
            const userEmailDisplay = document.getElementById('userEmailDisplay');
            
            if (isLoggedIn) {
                userEmailDisplay.innerHTML = `<i class="fas fa-user-circle me-2"></i>${adminName || adminEmail || 'Admin'}`;
                authControl.style.display = 'flex'; 
                authControl.classList.remove('d-none');
                authControl.classList.add('d-flex');
            } else {
                authControl.style.display = 'none';
                authControl.classList.remove('d-flex');
                authControl.classList.add('d-none');
            }
        }
        
        function logoutAdmin() {
            localStorage.removeItem('admin_is_logged_in');
            localStorage.removeItem('admin_email');
            localStorage.removeItem('admin_name');
            localStorage.removeItem('google_access_token'); 
            window.location.href = 'admin.html';
        }

        // *************************************************************
        // LOGIC SAO CHÉP CỤC BỘ (CLIENT-SIDE)
        // *************************************************************
        
        let sourceDirectoryHandle = null;
        let destinationDirectoryHandle = null;

        const SOURCE_DISPLAY_ID = 'sourceDirDisplay';
        const DEST_DISPLAY_ID = 'destDirDisplay';
        const FILENAMES_INPUT_ID = 'filenamesInput';
        const STATUS_DISPLAY_ID = 'statusDisplay';
        const COPY_BUTTON_ID = 'copyButton';

        /**
         * Mở hộp thoại chọn thư mục và lưu Directory Handle
         */
        async function selectDirectory(type) {
            if (!('showDirectoryPicker' in window)) {
                alert("Trình duyệt của bạn (Chỉ Chrome/Edge) không hỗ trợ chức năng này!");
                return;
            }

            try {
                // Yêu cầu người dùng chọn thư mục
                const handle = await window.showDirectoryPicker(); 
                const displayElement = document.getElementById(type === 'source' ? SOURCE_DISPLAY_ID : DEST_DISPLAY_ID);

                if (type === 'source') {
                    sourceDirectoryHandle = handle;
                    displayElement.innerHTML = `<i class="fas fa-check-circle me-1"></i>Nguồn: <strong>${handle.name}</strong>`;
                    displayElement.className = 'dir-display text-primary mt-2';
                } else {
                    destinationDirectoryHandle = handle;
                    displayElement.innerHTML = `<i class="fas fa-check-circle me-1"></i>Đích: <strong>${handle.name}</strong>`;
                    displayElement.className = 'dir-display text-success mt-2';
                }

                document.getElementById(STATUS_DISPLAY_ID).innerHTML = `<span class="text-success">✅ Đã cấp quyền cho: <strong>${handle.name}</strong></span>`;

            } catch (err) {
                if (err.name === 'AbortError') {
                    // Người dùng hủy, không làm gì
                } else {
                    console.error(err);
                    document.getElementById(STATUS_DISPLAY_ID).innerHTML = `<span class="text-danger">❌ Lỗi: ${err.message}</span>`;
                }
            }
        }

        /**
         * Thực hiện tìm kiếm, đọc file nguồn và ghi sang file đích
         */
        async function startCopyProcess() {
            const statusElement = document.getElementById(STATUS_DISPLAY_ID);
            const copyButton = document.getElementById(COPY_BUTTON_ID);

            if (!sourceDirectoryHandle || !destinationDirectoryHandle) {
                statusElement.innerHTML = '<span class="text-danger fw-bold"><i class="fas fa-exclamation-circle me-2"></i>Vui lòng chọn cả Thư mục Nguồn và Đích.</span>';
                return;
            }

            const filenamesRaw = document.getElementById(FILENAMES_INPUT_ID).value;
            if (!filenamesRaw.trim()) {
                statusElement.innerHTML = '<span class="text-danger fw-bold"><i class="fas fa-exclamation-circle me-2"></i>Vui lòng nhập danh sách tên file.</span>';
                return;
            }

            // Tách chuỗi và lọc tên file rỗng
            const filenamesToCopy = filenamesRaw.split(/[\n,]+/).map(name => name.trim()).filter(name => name.length > 0);
            
            if (filenamesToCopy.length === 0) {
                 statusElement.innerHTML = '<span class="text-danger fw-bold">Danh sách file trống.</span>';
                 return;
            }

            copyButton.disabled = true;
            copyButton.innerHTML = '<i class="fas fa-spinner fa-spin me-2"></i> Đang xử lý...';
            statusElement.innerHTML = '<span class="text-primary">⏳ Đang khởi động tiến trình...</span>';

            let successCount = 0;
            let failedList = [];
            let totalFiles = filenamesToCopy.length;

            for (let i = 0; i < totalFiles; i++) {
                const filename = filenamesToCopy[i];
                statusElement.innerHTML = `<span class="text-primary">⏳ Đang chép (${i + 1}/${totalFiles}): <strong>${filename}</strong>...</span>`;

                try {
                    // 1. Tìm và đọc File nguồn
                    const fileHandle = await sourceDirectoryHandle.getFileHandle(filename);
                    const file = await fileHandle.getFile();

                    // 2. Tạo File đích trong thư mục đích (Tạo nếu chưa tồn tại)
                    const destFileHandle = await destinationDirectoryHandle.getFileHandle(filename, { create: true });
                    
                    // 3. Ghi nội dung file nguồn vào file đích
                    const writable = await destFileHandle.createWritable();
                    await writable.write(file);
                    await writable.close();
                    
                    successCount++;

                } catch (error) {
                    console.error(`Lỗi sao chép ${filename}:`, error);
                    failedList.push(filename);
                }
            }

            // 4. Báo cáo kết quả cuối cùng
            let finalMessage = `<div class="text-success mb-2"><i class="fas fa-check-double me-2"></i>Hoàn tất! Thành công: <strong>${successCount}</strong> / ${totalFiles} file.</div>`;
            
            if (failedList.length > 0) {
                 finalMessage += `<div class="alert alert-warning mt-2 p-2" style="font-size: 0.9rem;">
                    <strong><i class="fas fa-exclamation-triangle me-2"></i>Các file lỗi (${failedList.length}):</strong><br>
                    ${failedList.join(', ')}
                 </div>`;
            }
            
            statusElement.innerHTML = finalMessage;
            copyButton.innerHTML = '<i class="fas fa-clone"></i> Thực Hiện Sao Chép';
            copyButton.disabled = false;
        }

        document.addEventListener('DOMContentLoaded', () => {
            updateHeaderUI();
        });
    </script>
</body>
</html>
